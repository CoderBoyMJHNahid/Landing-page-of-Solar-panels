jQuery.validator.setDefaults({
    errorContainer: ".formErrors",
    errorPlacement: function(r, e) {
        var t = jQuery(e).closest("form").find(".formErrors");
        if (t.length) {
            var n = e.attr("name")
              , a = jQuery.inArray(n, ["f_ddob", "f_mdob", "f_ydob"]) < 0 ? e.attr("id") : "f_age_18"
              , o = _wg_translations.validationNotes[n] || jQuery(e).prop("placeholder") || r.html() || "";
            if (!t.find("span.error").filter((function() {
                return $(this).text() === o
            }
            )).length) {
                var l = t.find("span.error." + a);
                !l.length && r.html().length ? (l = $('<span class="error ' + a + '">' + o + "</span>"),
                t.append(l)) : l.html(o)
            }
        } else
            r.insertAfter(e);
        return !1
    }
});
